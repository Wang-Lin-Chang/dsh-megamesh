# federation-spec — 跨机器联邦协议规范（v0.1）

> **状态：规范草案。全部跨机器内容未实测——本机单机环境无法构造真实网络，诚实标注。**
> 原则与 asmfs-spec 相同：协议在文件系统层，实现者可独立验证；**没实测不声称**。

## 1. 动机与边界

dsh-megamesh 的战场是一个**本地目录**（intent-queue / shared / done 的文件系统协议）。诚实边界第一条写着：**"单机共享文件系统，跨机器需共享磁盘+网络文件系统，未实测不声称"**。

本规范定义战场目录变成**分布式战场图**的协议路径，并严格区分：

- ✅ **可在单机模拟**：CRDT 合并函数、Merkle DAG 结构、Raft 任期锁的状态机逻辑——这些是纯函数/状态机，可以单机构造状态序列验证（**本规范发布时尚未做此模拟，见 §6**）。
- ❌ **必须真实网络实测**：网络分区、消息乱序、时钟漂移下的活性/安全性、跨机器带宽预算——**单机无法构造，不冒认**。

## 2. 分布式战场图（CRDT 战场）

- 战场目录 → **Merkle DAG**：每个文件是一个节点，内容寻址（SHA-256 digest），目录是聚合节点。
- 合并语义：**LWW（last-writer-wins）** 用于任务状态（意图是幂等的），**OR-Set** 用于任务队列（加入/完成可并发）。
- 关键不变量（沿用单机军法）：任务锁的互斥在分布式下**必须**由共识层保证——O_EXCL 文件锁在单机是互斥原语，跨机器**不是**（网络文件系统的锁语义不可靠）。

## 3. 任期锁升级：Raft

- 单机任期锁（brainId:pid:startSec:term + O_EXCL）→ **Raft 任期**：Leader 选举 + 日志复制。
- 为什么 Raft 而非更轻的投票：战场图的状态变更需要**全序**（decree 顺序影响军法判决），Raft 的日志全序恰好匹配。
- 保留的语义：任期锁的"租约判死"（lease）对应 Raft 的 **election timeout**；"三证据收养"对应 **log replay**。
- **未实测声明**：本规范描述状态机映射，未跑任何真实 Raft 实现——单机模拟（§6 第 1 项）完成后才可声称"逻辑验证"。

## 4. 战报压缩协议（跨机器带宽优化）

- 侦察兵"百字战报"压缩比 98.8%（E02 实测）在跨机器下变成**带宽协议**：网络比磁盘贵 1000 倍，战报摘要先行、全文按需回源。
- 协议草案：report-ack 两段式——先发摘要（~100 字节）获确认，再按需发全文。
- **未实测声明**：带宽收益需真实网络 RTT 数据，单机无意义，不预填数字。

## 5. 与单机协议的关系

- 跨机器联邦是**超集**：单机协议（租约/收养/军法/时间线）在每台机器上照常运行，战场图同步层叠加其上。
- 诚实边界不变：**单机行为的所有实验编号继续有效**；跨机器行为在新实验编号出现前全部标记"未实测"。

## 6. 待实测清单（诚实边界）

1. **单机模拟 Raft 状态机**（选举/日志复制/分区切换的逻辑序列）——可做，规范发布后第一优先
2. 真实双机（或双容器）CRDT 合并一致性
3. 网络分区下 decree 全序的一致性
4. 战报带宽协议的真实 RTT 收益

**这些项在实测数据出现前，任何"已实现跨机器联邦"的声称都不成立。**
