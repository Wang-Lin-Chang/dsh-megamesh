# byzantine-spec — 拜占庭容错协议规范（v0.1）

> **状态：规范草案。跨机器部分未实测，诚实标注；单机可实测部分见 dsh-megamesh E38。**
> 原则与 asmfs-spec 相同：协议在文件系统层，实现者可独立验证；**没实测不声称**。

## 1. 动机与边界

dsh-megamesh 的联邦脑有**崩溃容错换脑**（任期锁 + 租约判死 + 三证据收养）——它假设 Agent 会崩、会死循环，但**不会恶意伪造**。

拜占庭容错是下一个台阶：面对**会撒谎的 Agent**。本规范定义撒谎的三种形态与协议层对策，并严格区分：

- ✅ **单机可实测**（E38 已验）：Ed25519 签名/验签、2/3 背书计数、死因分类逻辑——密码学原语与协议逻辑不需要网络。
- ❌ **待社区实测**：跨机器合谋（节点串通）、网络分区下的背书时序、双花战报的跨机裁决——本机无法构造真实网络环境，**不冒认**。

## 2. 签名战报（Report Signing）

- 战报规范化：`JSON.stringify(report, Object.keys(report).sort())` 作摘要（键序无关的规范化）。
- 签名算法：**Ed25519**（node:crypto 原生，64 字节签名）。
- 语义：签名 = 权威性证明（这份战报确为此 Agent 所写且未被改动）。

**E38 实测数据（单机）**：合法签名放行 ✓ · 篡改战报拦截 ✓ · 伪造签名拦截 ✓ · 错误公钥拦截 ✓。

## 3. 背书协议（Endorsement）

- 验证者组 N（≥3），quorum = ⌊2N/3⌋ + 1（严格 2/3，进一取整）。
- 战报需 ≥ quorum 个验证者验签通过才入账。
- **E38 实测**：N=5 时 4/5 通过、3/5 与 2/5 均不通过——严格 2/3 语义验证。
- **加权背书（待社区实测）**：验证者按历史可靠性加权，权重分配机制（如 τ(n) 分层）**需跨机器数据校准——规范不预填系数，等实测**。

## 4. 死因代码扩展（autopsy-spec）

| 代码 | 名称 | 定义 | 检测 |
|---|---|---|---|
| D-10 | SIGNATURE_FORGERY | 战报验签失败 | 单机可测（E38） |
| D-11 | DOUBLE_REPORT | 同 taskId 两份不同战报 | 单机可测（E38） |
| D-12 | BYZANTINE_COLLUSION | 背书成组分裂（部分全真部分全假） | 迹象检测单机可测；**跨机器确证待实测** |

## 5. 与现有军法的关系

- 战报仍需过军法（lawCourt 0 违规）——签名是**权威性前提**，军法是**语义约束**，两者正交。
- 崩溃容错（三证据收养）与拜占庭容错（签名背书）**分层**：先验权威性，再验语义。

## 6. 待社区实测清单（诚实边界）

1. 跨机器合谋确证（D-12 的完整判定需网络拓扑数据）
2. 网络分区下背书时序（quorum 在分区中的活性/安全性）
3. 加权系数校准（τ(n) 分层权重的数据来源）
4. 双花战报的跨机裁决一致性

**这些项在实测数据出现前，任何"已实现拜占庭容错"的声称都不成立。**
